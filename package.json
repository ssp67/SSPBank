{
  "name": "bank-schema-utils",
  "version": "1.0.0",
  "description": "Helpers for the Bank schema repository (mermaid export)",
  "scripts": {
    "export:erd:png": "mmdc -i docs/erd.mmd -o docs/erd.png -w 1200 -H 800",
    "export:erd:svg": "mmdc -i docs/erd.mmd -o docs/erd.svg",
    "export:erd": "npm run export:erd:png && npm run export:erd:svg"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^10.0.0"
  }
}
